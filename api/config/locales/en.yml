en:
  sessions:
    invalid: Invalid email or password
    rate_limit: Try again later
  passwords:
    invalid: Passwords did not match.
    expired: Password reset link is invalid or has expired.
  activerecord:
    models:
      habit: Habit
      user: User
      task: Task
    attributes:
      habit:
        title: Title
        description: Description
        rrule: RRule
        tzid: Timezone 
      task:
        title: Title
        description: Description
      user:
        email: Email
        password: Password 

